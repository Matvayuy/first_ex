# Работа с кодом
## Запуск приложения
```bash
go run main.go
```
## Build
```bash
go build main.go
```
# Работа с git
- `git help` - показывает, как пользоваться утилитой git
- `git status` -
- `git add` . -
- `git commit` -m <comment> -
- `git push` -
- `git pull` - 
- `git log` - 
- `git checkout` <метка> -
